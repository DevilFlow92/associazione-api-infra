apiVersion: v1
kind: Service
metadata:
  name: associazione-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: associazione-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP